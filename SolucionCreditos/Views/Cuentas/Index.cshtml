@model List<Cuenta>
@using SolucionCreditos.Entities;

<div class="mb-3">
    <a class="btn btn-info" asp-controller="Cuentas" asp-action="Crear" asp-route-id="@ViewBag.idCliente">Crear cuenta</a>
</div>

<table class="table">
    <thead class="table-dark">
        <tr>
            <th>Id</th>
            <th>Cliente</th>
            <th>Cuentas</th>
            <th>Operaciones</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
        <tr>
            <td>@item.IdCuenta</td>
            <td>@item.NumeroCuenta</td>
            <td>@item.Saldo</td>
            <td>
                <a class="btn btn-primary" asp-controller="Depositos" asp-action="Index" asp-route-id="@item.IdCuenta">Deposito</a>
                <a class="btn btn-primary" asp-controller="Retiros" asp-action="Index" asp-route-id="@item.IdCuenta">Retiro</a>
            </td>

        </tr>
        }
    </tbody>
</table>
